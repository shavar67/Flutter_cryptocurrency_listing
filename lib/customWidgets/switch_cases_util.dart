import 'package:flutter/material.dart';

class SwitchCases extends StatelessWidget {
  final int cases;

  const SwitchCases({Key key, @required this.cases}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
